# V2.4 Integration Contract

## 创作权限

- Research Director：事实、来源、SCRIPT_BLOCKER、历史边界。
- Art Director：风格、World、MASTER、Motif。
- Visual Director：Beat/Shot/Grade/Role/Render Medium/Grammar/Complexity/Handle/Precision/模板选择。
- H3 Prompt Director：编译，不重新导演。
- Visual Critic：看真实视觉结果后 QA/RETURN。
- Production Engine：Schema、状态、Budget、Approval、Asset Gate、API、技术 QA、粗剪、Manifest；没有创作权限。

## 硬门槛

1. 未解决 SCRIPT_BLOCKER → 禁止 paid generation。
2. 缺 blocking asset → 相关 Shot 禁止 paid generation。
3. World 未 LOCK → Production Keyframe / H3 禁止批量开始。
4. Production Keyframe 不过 → H3 禁止开始。
5. 未明确 Approval → H3 禁止真实请求。
6. Real call 必须显式 `--approved`。
7. 真实请求前必须先 dry-run。
8. 非 H3 Render Medium 不应被模板系统强制 H3 化。
9. 重要文字/证据/Logo/UI 进入 REAL_EVIDENCE / HYPERFRAMES / Precision，不能为模板而造假。
10. Critic 未看到真实图/视频 → 不得精确打分。
11. Whole-film QA 必须基于 Rough Cut。
12. T01–T21 只提供 Pattern，不替代 World Bible、Visual Motif 和导演判断。
